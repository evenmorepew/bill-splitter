name: Java CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Set up JDK 11
      uses: actions/Setup Java Action@v1
      with:
        version: 11
    - name: Build with Maven
      run: mvn -B clean package --file pom.xml
