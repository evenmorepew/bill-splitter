name: Java CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: My Hello World
      run: |
        echo "${{secrets.TEST_SECRET}}"
        echo "${{vars.TEST_VAR}}"
