name: Java CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    environment: Test
    steps:
    - name: My Hello World
      env:
        MY_SECRET: ${{ secrets.TEST_SECRET }}
        MY_VAR: ${{ vars.TEST_VAR }}
        MY_KEY: ${{ secrets.SSH_KEY }}
      run: |
        echo "$MY_KEY" > id_rsa
        chmod 600 id_rsa
        ls -l
